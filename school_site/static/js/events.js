var events = document.getElementsByClassName("event");

        for (var i = 0; i < events.length; i++) {
            events[i].style.backgroundColor = "lightblue";
            events[i].style.padding = "10px";
            events[i].style.marginBottom = "10px";
            events[i].style.boxShadow = "5px 5px 10px rgba(0, 0, 0, 0.3)";
        }